[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fastq-chunk"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "dnaio",
    "numpy",
]

[project.urls]
Repository = "https://github.com/meekrob/fastq_chunk"
